# Shared upload player

All Guitar Pro uploads generated by `analyzer/build_tab_from_gp5.py` already reference `tabs/generated-tab-player.js`. The shared player is therefore the single standard behavior for current and future uploaded tabs. Instrument switching must preserve the global playback position instead of resetting to bar 1.
